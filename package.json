{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm:server\" \"npm:client\"",
    "client": "npm --prefix client run dev",
    "server": "nodemon server/index.js",
    "build": "echo 'Use client/build for frontend & node server for API'",
    "start": "node server/index.js",
    "lint": "eslint"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongoose": "^8.19.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "eslint": "^9",
    "nodemon": "^3.1.10"
  }
}
